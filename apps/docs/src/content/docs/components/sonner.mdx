---
title: Sonner
description: An opinionated toast component for Solid.

link:
  doc: https://oc1s.github.io/somo
  api: https://oc1s.github.io/somo/toaster/#api-reference
---

<ComponentPreview name="sonner-demo">

```tsx file=<rootDir>/src/registry/examples/sonner-demo.tsx

```

</ComponentPreview>

## Installation

<ComponentInstallation>

<Step>
  <StepItem>Install the following dependencies:</StepItem>

```bash
npm install somoto @kobalte/core
```

<StepItem>Copy and paste the following code into your project:</StepItem>

  <ComponentSourceTabs>

    ```tsx title="components/ui/sonner" file=<rootDir>/src/registry/ui/sonner.tsx

    ```

    ```tsx title="components/ui/sonner" file=<rootDir>../../unocss-playground/src/registry/ui/sonner.tsx

    ```

  </ComponentSourceTabs>

<StepItem>Add the Toaster component</StepItem>

```tsx title="src/app.tsx" {1,9}
import { Toaster } from "@/components/ui/sonner"

export default function App() {
  return (
    <Router
      root={(props) => (
        <Suspense>
          {props.children}
          <Toaster />
        </Suspense>
      )}
    >
      <FileRoutes />
    </Router>
  )
}
```

</Step>

</ComponentInstallation>

## Usage

```tsx
import { toast } from "somoto"
```

```tsx
toast("Event has been created.")
```
