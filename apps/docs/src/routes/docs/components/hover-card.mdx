---
title: Hover Card
description: For sighted users to preview content available behind a link.
component: true
kobalte:
    link: https://kobalte.dev/docs/core/components/hover-card
    api: https://kobalte.dev/docs/core/components/hover-card#api-reference
---

<ComponentExample src="/src/components/examples/hover-card/demo.tsx">
	<HoverCardDemo />
</ComponentExample>

## Installation

<Steps>

<Step>Copy and paste the following code into your project.</Step>

<ComponentSource src="/src/components/ui/hover-card.tsx" />

<Step>Update the import paths to match your project setup.</Step>

<Step>Update `uno.config.ts`</Step>

Add the following animations to your `uno.config.ts` file:

```tsx title="uno.config.ts" {3-28}
export default defineConfig({
	theme: {
		animation: {
			keyframes: {
				"content-show":
					"{ from { opacity: 0; transform: scale(0.96) } to { opacity: 1; transform: scale(1) }}",
				"content-hide":
					"{ from { opacity: 1; transform: scale(1) } to { opacity: 0; transform: scale(0.96) }}",
			},
			durations: {
				"content-show": "200ms",
				"content-hide": "200ms",
			},
			timingFns: {
				"content-show": "ease-out",
				"content-hide": "ease-in",
			},
			counts: {
				"content-hide": "forwards",
			},
		},
	},
})
```

</Steps>

## Usage

```tsx
import { HoverCard, HoverCardContent, HoverCardTrigger } from "~/components"
```

```tsx
<HoverCard>
	<HoverCardTrigger>Hover</HoverCardTrigger>
	<HoverCardContent>
		SolidJS - Simple and performant reactivity for building user interfaces.
	</HoverCardContent>
</HoverCard>
```
