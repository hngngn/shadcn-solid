---
import { DocsLayout } from "@/layouts"
import { getEntry } from "astro:content"

const doc = await getEntry("docs", "index")
const { Content, headings } = await doc.render()
---

<DocsLayout doc={doc.data} headings={headings}>
	<Content />
</DocsLayout>
