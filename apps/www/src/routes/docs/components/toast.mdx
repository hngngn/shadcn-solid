---
title: Toast
description: A succinct message that is displayed temporarily.
component: true
kobalte:
    link: https://kobalte.dev/docs/core/components/toast
    api: https://kobalte.dev/docs/core/components/toast#api-reference
---

import {
    ComponentExample,
    ComponentSource,
    Alert,
    AlertDescription,
} from "~/components"

<Alert>
<AlertDescription>

**Note**: You might notice a bug in the examples when swiping or pausing, but it works perfectly fine, see [here](https://stackblitz.com/edit/shadcn-solid-toast).

</AlertDescription>
</Alert>

<ComponentExample src="/src/components/examples/toast/demo.tsx">
    <ToastDemo />
</ComponentExample>

## Installation

<Steps>

<Step>Copy and paste the following code into your project.</Step>

<ComponentSource src="/src/components/ui/textfield.tsx" />

<Step>Update the import paths to match your project setup.</Step>

<Step>Update css variable</Step>

Add the following animations to your global css file:

```css
:root {
    --viewport-padding: 16px;
}
```

<Step>Update `uno.config.ts`</Step>

Add the following animations to your `uno.config.ts` file:

```tsx title="uno.config.ts" {3-18}
export default defineConfig({
    theme: {
        animation: {
            keyframes: {
                "slide-in":
                    "{ from { transform: translateY(calc(100% + var(--viewport-padding))) } to { transform: translateY(0) }}",
                "slide-out":
                    "{ from { transform: translateY(var(--kb-toast-swipe-end-y)) } to { transform: translateY(calc(100% + var(--viewport-padding))) }}",
            },
            durations: {
                "slide-in": "150ms",
                "slide-out": "100ms",
            },
            timingFns: {
                "slide-in": "cubic-bezier(0.16, 1, 0.3, 1)",
                "slide-out": "ease-out",
            },
        },
    },
})
```

</Steps>

## Examples

### Destructive

<ComponentExample src="/src/components/examples/toast/destructive.tsx">
    <ToastDestructive />
</ComponentExample>
