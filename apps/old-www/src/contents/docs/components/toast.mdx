---
title: Toast
description: A succinct message that is displayed temporarily.
component: true
link:
  doc: https://kobalte.dev/docs/core/components/toast
  api: https://kobalte.dev/docs/core/components/toast#api-reference
---

<ComponentPreview name="toast-demo" />

## Installation

<Tabs defaultValue="cli">

<TabsList>
  <TabsTrigger value="cli">CLI</TabsTrigger>
  <TabsTrigger value="manual-tw">Manual - TailwindCSS</TabsTrigger>
  <TabsTrigger value="manual-uno">Manual - UnoCSS</TabsTrigger>
  <TabsIndicator variant="underline" />
</TabsList>

<TabsContent value="cli">

```bash
npx shadcn-solid@latest add toast
```

</TabsContent>

<TabsContent value="manual-tw">

<Steps>

<Step>Install the following dependencies:</Step>

```bash
npm install @kobalte/core
```

<Step>Copy and paste the following code into your project.</Step>

<ComponentSource name="toast" css="tailwindcss" />

<Step>Add the `ToastRegion` and `ToastList` component</Step>

```tsx title="src/app.tsx" {1,11-13}
import { ToastRegion, ToastList } from "@/components/ui/toast"

const App = () => {
  return (
    <Router
      root={props => (
          <Suspense>
            <ColorModeScript />
            <ColorModeProvider>
              {props.children}
              <ToastRegion>
                <ToastList />
              </ToastRegion>
            </ColorModeProvider>
          </Suspense>
      )}
    >
      <FileRoutes />
    </Router>
  );
};

export default App;
```

</Steps>

</TabsContent>

<TabsContent value="manual-uno">

<Steps>

<Step>Install the following dependencies:</Step>

```bash
npm install @kobalte/core
```

<Step>Copy and paste the following code into your project.</Step>

<ComponentSource name="toast" css="unocss" />

<Step>Add the `ToastRegion` and `ToastList` component</Step>

```tsx title="src/app.tsx" {1,11-13}
import { ToastRegion, ToastList } from "@/components/ui/toast"

const App = () => {
  return (
    <Router
      root={props => (
          <Suspense>
            <ColorModeScript />
            <ColorModeProvider>
              {props.children}
              <ToastRegion>
                <ToastList />
              </ToastRegion>
            </ColorModeProvider>
          </Suspense>
      )}
    >
      <FileRoutes />
    </Router>
  );
};

export default App;
```

</Steps>

</TabsContent>

</Tabs>

## Usage

```tsx
import { toaster } from "@kobalte/core";
import {
  Toast,
  ToastContent,
  ToastProgress,
  ToastTitle
} from "@/components/ui/toast";
```

```tsx
toaster.show(props => (
  <Toast toastId={props.toastId}>
    <ToastContent>
      <ToastTitle>Toast</ToastTitle>
    </ToastContent>
    <ToastProgress />
  </Toast>
))
```

## Examples

### Destructive

<ComponentPreview name="toast-destructive" />
