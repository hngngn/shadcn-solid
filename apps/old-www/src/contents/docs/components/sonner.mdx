---
title: Sonner
description: An opinionated toast component for Solid.
component: true
link:
  doc: https://solid-sonner.vercel.app
---

<ComponentPreview name="sonner-demo" />

## Installation

<Tabs defaultValue="cli">

<TabsList>
  <TabsTrigger value="cli">CLI</TabsTrigger>
  <TabsTrigger value="manual-tw">Manual - TailwindCSS</TabsTrigger>
  <TabsTrigger value="manual-uno">Manual - UnoCSS</TabsTrigger>
  <TabsIndicator variant="underline" />
</TabsList>

<TabsContent value="cli">

```bash
npx shadcn-solid@latest add sonner
```

</TabsContent>

<TabsContent value="manual-tw">

<Steps>

<Step>Install the following dependencies:</Step>

```bash
npm install solid-sonner
```

<Step>Copy and paste the following code into your project.</Step>

<ComponentSource name="sonner" css="tailwindcss" />

<Step>Add the Toaster component</Step>

```tsx title="src/app.tsx" {1,11}
import { Toaster } from "@/components/ui/sonner"

const App = () => {
  return (
    <Router
      root={props => (
          <Suspense>
            <ColorModeScript />
            <ColorModeProvider>
              {props.children}
              <Toaster />
            </ColorModeProvider>
          </Suspense>
      )}
    >
      <FileRoutes />
    </Router>
  );
};

export default App;
```

</Steps>

</TabsContent>

<TabsContent value="manual-uno">

<Steps>

<Step>Install the following dependencies:</Step>

```bash
npm install solid-sonner
```

<Step>Copy and paste the following code into your project.</Step>

<ComponentSource name="sonner" css="unocss" />

<Step>Add the Toaster component</Step>

```tsx title="src/app.tsx" {1,11}
import { Toaster } from "@/components/ui/sonner"

const App = () => {
  return (
    <Router
      root={props => (
          <Suspense>
            <ColorModeScript />
            <ColorModeProvider>
              {props.children}
              <Toaster />
            </ColorModeProvider>
          </Suspense>
      )}
    >
      <FileRoutes />
    </Router>
  );
};

export default App;
```

</Steps>

</TabsContent>

</Tabs>

## Usage

```tsx
import { toast } from "sonner"
```

```tsx
toast("Event has been created.")
```