---
title: OTP Field
description: An accessible and customizable OTP Input component.
component: true
link:
  doc: https://corvu.dev/docs/primitives/otp-field/
  api: https://corvu.dev/docs/primitives/otp-field#api-reference
---

<ComponentPreview name="otp-field-demo" >

```tsx file=<rootDir>/src/registry/tailwindcss/examples/otp-field-demo.tsx

```

</ComponentPreview>

## Installation

<ComponentInstallation >

```bash
npx shadcn-solid@latest add otp-field
```

<Step >

<StepItem>Install the following dependencies:</StepItem>

```bash
npm install @corvu/otp-field
```

<StepItem>Copy and paste the following code into your project:</StepItem>
```tsx file=<rootDir>/src/registry/tailwindcss/ui/otp-field.tsx
```

<StepItem>Update config:</StepItem>

```js title="tailwind.config.cjs" {5-18}
/** @type {import('tailwindcss').Config} */
module.exports = {
  theme: {
    extend: {
      keyframes: {
        "accordion-down": {
          from: { height: 0 },
          to: { height: "var(--kb-accordion-content-height)" },
        },
        "accordion-up": {
          from: { height: "var(--kb-accordion-content-height)" },
          to: { height: 0 },
        },
      },
      animation: {
        "accordion-down": "accordion-down 0.2s ease-out",
        "accordion-up": "accordion-up 0.2s ease-out",
      },
    },
  },
}
```

</Step>

<Step >

<StepItem>Install the following dependencies:</StepItem>

```bash
npm install @corvu/otp-field
```

<StepItem>Copy and paste the following code into your project:</StepItem>
```tsx file=<rootDir>/../packages/unocss/ui/otp-field.tsx
```

<StepItem>Update config:</StepItem>

```ts title="uno.config.ts" {4-15}
export default defineConfig({
  themes: {
    animation: {
      keyframes: {
        "caret-blink": "{ 0%,70%,100% { opacity: 1 } 20%,50% { opacity: 0 } }",
      },
      timingFns: {
        "caret-blink": "ease-out",
      },
      durations: {
        "caret-blink": "1.25s",
      },
      counts: {
        "caret-blink": "infinite",
      },
    },
  },
})
```

</Step>

</ComponentInstallation>

## Usage

```tsx
import {
  OTPField,
  OTPFieldGroup,
  OTPFieldInput,
  OTPFieldSeparator,
  OTPFieldSlot,
} from "@/components/ui/otp-field"
```

```tsx
<OTPField maxLength={6}>
  <OTPFieldInput />
  <OTPFieldGroup>
    <OTPFieldSlot index={0} />
    <OTPFieldSlot index={1} />
    <OTPFieldSlot index={2} />
  </OTPFieldGroup>
  <OTPFieldSeparator />
  <OTPFieldGroup>
    <OTPFieldSlot index={3} />
    <OTPFieldSlot index={4} />
    <OTPFieldSlot index={5} />
  </OTPFieldGroup>
</OTPField>
```

## Exmaples

### Pattern

Use the `pattern` prop to define a custom pattern for the OTP field.

<ComponentPreview name="otp-field-with-pattern-demo" >

```tsx file=<rootDir>/src/registry/tailwindcss/examples/otp-field-with-pattern-demo.tsx

```

</ComponentPreview>

### Controlled

You can use the `value` and `onValueChange` props to control the input value.

<ComponentPreview name="otp-field-with-controlled-demo" >

```tsx file=<rootDir>/src/registry/tailwindcss/examples/otp-field-with-controlled-demo.tsx

```

</ComponentPreview>
