---
title: Toast
description: A succinct message that is displayed temporarily.
component: true
link:
  doc: https://kobalte.dev/docs/core/components/toast
  api: https://kobalte.dev/docs/core/components/toast#api-reference
---

import ComponentInstallation from "@/components/component-installation";
import ComponentPreview from "@/components/component-preview";

<ComponentPreview name="toast-demo" client:only="solid-js">
<Fragment slot="code">
```tsx file=<rootDir>/src/examples/toast-demo.tsx
```
</Fragment>
</ComponentPreview>

## Installation

<ComponentInstallation client:only="solid-js">

<Fragment slot="cli">
```bash
npx shadcn-solid@latest add toast
```
</Fragment>

<Step slot="tw">

<StepItem>Install the following dependencies:</StepItem>
```bash
npm install @kobalte/core
```

<StepItem>Copy and paste the following code into your project:</StepItem>
```tsx file=<rootDir>/../packages/tailwindcss/ui/toast.tsx
```

</Step>

<Step slot="uno">

<StepItem>Install the following dependencies:</StepItem>
```bash
npm install @kobalte/core
```

<StepItem>Copy and paste the following code into your project:</StepItem>
```tsx file=<rootDir>/../packages/unocss/ui/toast.tsx
```

</Step>

</ComponentInstallation>

## Usage

```tsx
import { toaster } from "@kobalte/core";
import {
  Toast,
  ToastContent,
  ToastProgress,
  ToastTitle
} from "@/components/ui/toast";
```

```tsx
toaster.show(props => (
  <Toast toastId={props.toastId}>
    <ToastContent>
      <ToastTitle>Toast</ToastTitle>
    </ToastContent>
    <ToastProgress />
  </Toast>
))
```

```tsx title="src/main.tsx" {1,11-13}
import { ToastRegion, ToastList } from "@/components/ui/toast"
 
const App = () => {
  return (
    <Router
      root={props => (
          <Suspense>
            <ColorModeScript />
            <ColorModeProvider>
              {props.children}
              <ToastRegion>
                <ToastList />
              </ToastRegion>
            </ColorModeProvider>
          </Suspense>
      )}
    >
      <FileRoutes />
    </Router>
  );
};
 
export default App;
```

## Examples

### Destructive

<ComponentPreview name="toast-destructive" client:only="solid-js">
<Fragment slot="code">
```tsx file=<rootDir>/src/examples/toast-destructive.tsx
```
</Fragment>
</ComponentPreview>
