---
title: Sonner
description: An opinionated toast component for Solid.
component: true
link:
  doc: https://solid-sonner.vercel.app
---

import ComponentInstallation from "@/components/component-installation";
import ComponentPreview from "@/components/component-preview";

<ComponentPreview name="sonner-demo" client:only="solid-js">
<Fragment slot="code">
```tsx file=<rootDir>/src/examples/sonner-demo.tsx
```
</Fragment>
</ComponentPreview>

## Installation

<ComponentInstallation client:only="solid-js">

<Fragment slot="cli">
```bash
npx shadcn-solid@latest add sonner
```
</Fragment>

<Step slot="tw">

<StepItem>Install the following dependencies:</StepItem>
```bash
npm install solid-sonner
```

<StepItem>Copy and paste the following code into your project:</StepItem>
```tsx file=<rootDir>/node_modules/@repo/tailwindcss/ui/sonner.tsx
```

</Step>

<Step slot="uno">

<StepItem>Install the following dependencies:</StepItem>
```bash
npm install solid-sonner
```

<StepItem>Copy and paste the following code into your project:</StepItem>
```tsx file=<rootDir>/node_modules/@repo/unocss/ui/sonner.tsx
```

</Step>

</ComponentInstallation>

## Usage

```tsx
import { toast } from "solid-sonner"
```

```tsx
toast("Event has been created.")
```

```tsx title="src/main.tsx" {1,11}
import { Toaster } from "@/components/ui/sonner"
 
const App = () => {
  return (
    <Router
      root={props => (
          <Suspense>
            <ColorModeScript />
            <ColorModeProvider>
              {props.children}
              <Toaster />
            </ColorModeProvider>
          </Suspense>
      )}
    >
      <FileRoutes />
    </Router>
  );
};
 
export default App;
```
