---
title: Sonner
description: An opinionated toast component for Solid.
component: true
link:
  doc: https://solid-sonner.vercel.app
---

import ComponentInstallation from "@/components/component-installation";
import ComponentPreview from "@/components/component-preview";
import RawCode from "@/components/raw-code.tsx";
import TwRawCode from "@repo/tailwindcss/default/sonner?raw";
import UnoRawCode from "@repo/unocss/default/sonner?raw";

<ComponentPreview name="sonner-demo" client:only="solid-js" />

## Installation

<ComponentInstallation client:only="solid-js">

<Fragment slot="cli">
```bash
npx shadcn-solid@latest add sonner
```
</Fragment>

<Step slot="tw">

<StepItem>Install the following dependencies:</StepItem>
```bash
npm install solid-sonner
```

<StepItem>Copy and paste the following code into your project:</StepItem>
<RawCode code={TwRawCode} client:only="solid-js" />

</Step>

<Step slot="uno">

<StepItem>Install the following dependencies:</StepItem>
```bash
npm install solid-sonner
```

<StepItem>Copy and paste the following code into your project:</StepItem>
<RawCode code={UnoRawCode} client:only="solid-js" />

</Step>

</ComponentInstallation>

## Usage

```tsx
import { toast } from "sonner"
```

```tsx
toast("Event has been created.")
```

```tsx title="src/main.tsx" {1,11}
import { Toaster } from "@/components/ui/sonner"
 
const App = () => {
  return (
    <Router
      root={props => (
          <Suspense>
            <ColorModeScript />
            <ColorModeProvider>
              {props.children}
              <Toaster />
            </ColorModeProvider>
          </Suspense>
      )}
    >
      <FileRoutes />
    </Router>
  );
};
 
export default App;
```